<div class="row">
  <div class="col-md-12">
    {{#ui/ui-box as |b|}}
      {{#b.body}}
        <div class="row">
          <div class="col-md-12">
            {{#bs-form class="table-filters" onSubmit=(action "submit") formLayout="inline" model=recordQuery as |form|}}
              {{yield form}}
              <div class="table-filters-actions">
                {{#if hasFiltered }}
                  <a href="#" class="btn btn-secondary" {{action "reset"}}>
                    {{fa-icon "times"}}
                  </a>
                {{/if}}

                {{bs-button defaultText="Filter" type="primary" buttonType="submit"}}
              </div>

            {{/bs-form}}
          </div>
        </div>
      {{/b.body}}
    {{/ui/ui-box}}
  </div>
</div>

